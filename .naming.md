# Naming Decision

Detection date: 2026-07-20

| Registry | Chosen name | Detection result |
|----------|-------------|------------------|
| npm | `@shipzy/sdk` | https://registry.npmjs.org/@shipzy%2Fsdk → 404 (available); org `shipzy` reservation TBD by user during Chunk 8 |
| Packagist | `shipzy/sdk` | https://packagist.org/packages/shipzy/sdk.json → 404 (available) |
| PyPI | `shipzy-sdk` | https://pypi.org/pypi/shipzy-sdk/json → 404 (available) |
| Go proxy | `github.com/alaikis/shipzy-sdks/go` | fixed (repo path) |

**Decision:** Use the primary vendor set. All four names align on `shipzy` / `@shipzy` / `shipzy/sdk` / `shipzy-sdk`.

**Fallback tier (only if primary is claimed before first release push):**

| Registry | Fallback |
|----------|----------|
| npm | `@shipzy-io/sdk` |
| Packagist | `shipzy-sdks/sdk` |
| PyPI | `shipzy-sdk-py` |
