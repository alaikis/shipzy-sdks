[package]
name = "zymeup-sdk"
version = "2.0.2"
edition = "2021"
description = "Official Zymeup logistics platform SDK for Rust"
license = "MIT"
repository = "https://github.com/alaikis/shipzy-sdks"
keywords = ["zymeup", "logistics", "epod", "sdk"]
categories = ["api-bindings"]

[dependencies]
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
urlencoding = "2"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tokio-test = "0.4"
